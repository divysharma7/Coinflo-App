import 'package:flutter/material.dart';

class AppRadius {
  AppRadius._();

  static const BorderRadius xxs = BorderRadius.all(Radius.circular(4));
  static const BorderRadius xs = BorderRadius.all(Radius.circular(6));
  static const BorderRadius s = BorderRadius.all(Radius.circular(8));
  static const BorderRadius sm = BorderRadius.all(Radius.circular(10));
  static const BorderRadius base = BorderRadius.all(Radius.circular(12));
  static const BorderRadius md = BorderRadius.all(Radius.circular(14));
  static const BorderRadius mdLg = BorderRadius.all(Radius.circular(16));
  static const BorderRadius lg = BorderRadius.all(Radius.circular(20));
  static const BorderRadius lgXl = BorderRadius.all(Radius.circular(24));
  static const BorderRadius xlSm = BorderRadius.all(Radius.circular(26));
  static const BorderRadius xl = BorderRadius.all(Radius.circular(28));
  static const BorderRadius xxl = BorderRadius.all(Radius.circular(30));
  static const BorderRadius pill = BorderRadius.all(Radius.circular(100));
  static const BorderRadius full = BorderRadius.all(Radius.circular(999));
}
